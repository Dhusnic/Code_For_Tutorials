@{
    RemoteHost = '10.0.4.132'
    User = 'root'
    Port = 22

    # Put the SSH password here if you want the script to use it automatically.
    Password = 'hanTZ123'

    # Leave empty to use password auth from Password above.
    # Set a key path here if you prefer key-based auth instead.
    IdentityFile = ''
}
