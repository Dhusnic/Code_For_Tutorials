# Legacy Web Baseline

This folder contains the original Python/FastAPI + static UI application moved from repository root.

It remains the behavior baseline while desktop Go modules are migrated to native parity.

Run:

```powershell
python main.py
```

Default URL:

- `http://localhost:8000`
